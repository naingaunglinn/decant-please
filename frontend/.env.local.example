# Laravel API base — the storefront appends /v1/{shop}. Also decides where uploaded
# images are optimized from (see next.config.ts).
NEXT_PUBLIC_API_URL=http://localhost:8010/api

# Which shop this storefront is (multi-tenancy Step 24): the {shop} segment folded
# into every API path. Each Vercel project sets its own; must match a shops.slug row
# (locally, the seeded default). Unset falls back to decant-please — a deliberate
# dev convenience that a future guard will turn into a build failure (CHANGELOG v24).
NEXT_PUBLIC_SHOP_SLUG=decant-please

# Public site URL — canonical/OG metadata, sitemap, robots.
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Production only: the Cloudflare R2 public image host, added to next.config.ts's image
# remotePatterns. Leave unset locally — in dev, images are served from the API host's
# /storage path, which is already allow-listed.
# NEXT_PUBLIC_IMAGE_URL=https://images.cornerarea.me
